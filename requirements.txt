lxml>=5.0
openpyxl>=3.1
xlrd>=2.0          # legacy .xls invoice parsing only (Milestone One V2)
pdfplumber>=0.11
pymupdf>=1.24      # imported as `fitz` — Tier-2 vision invoice parser page raster

# web app (server/) — the local UI + API
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# OCR fallback for image-only / scanned invoices (Milestone Zero)
# System packages also required: tesseract-ocr, poppler-utils
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0
