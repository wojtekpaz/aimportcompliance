lxml>=5.0
openpyxl>=3.1
pdfplumber>=0.11

# web app (server/) — the local UI + API
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
jinja2>=3.1            # autoescaped server-side templates (survey review page)

# OCR fallback for image-only / scanned invoices (Milestone Zero)
# System packages also required: tesseract-ocr, poppler-utils
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0
