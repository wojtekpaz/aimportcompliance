{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "aptPackages": ["tesseract-ocr", "tesseract-ocr-pol", "poppler-utils"],
    "startCommand": "uvicorn server.app:app --host 0.0.0.0 --port $PORT"
  }
}
